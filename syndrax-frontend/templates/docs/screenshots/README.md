# Screenshots for Oleg (how to capture)

PR #8 review helpers. Capture these after `SITE_PORT=3002 node dev-server.mjs`:

1. **Library** — `http://localhost:3002/templates.html`  
   - Should show **44 shown · 44 total**  
   - Browse a buyer message + a listing niche

2. **Install all** — same page, left **Install all packs**  
   - Status line: packs added / skipped

3. **Login local** — `http://localhost:3002/login`  
   - Bottom button **Enter app (local preview)**

4. **Workspace strip** — after local preview → **Workspace**  
   - Templates bar: Message Tool · Description Builder · Workflow packs

5. **Workflows clean panel** — **Workflows**  
   - Library & saved (grouped Add packs + clean Saved rows)

Drop PNGs here when captured (`templates/docs/screenshots/`).
